# AgentFrame Managed Run

This is a managed unattended run. The watcher has set `AGENTFRAME_MANAGED_RUN=1`.

- Workspace: `{workspace}`
- Deployment: `{deployment_id}`
- Project: `{project}`
- Automation: `{automation_id}`
- Task file: `{task_file}`
- Required result file: `{result_file}`

Start by reading `AGENTS.daemon.md`, then the task file and the project automation contract it identifies. Execute one task inside the charter. Always write the required result file exactly as specified, then exit.
