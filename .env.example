# Root MarketingOS environment template.
# Copy to .env and fill only the keys your local runtime needs.

# Model runtime
GEMINI_API_KEY=

# Composio, optional until the chosen MCP setup requires headers.
COMPOSIO_API_KEY=
COMPOSIO_MCP_URL=
