# Sources — {name}

Raw, immutable inputs: call recordings and transcripts, recruiter mail, secondary JDs, offer letters,
screenshots. Files here are never edited, only registered below.

**Never prep from this folder.** Sources are mined once; the conclusions get promoted into the living
dossiers (`../company-brief.md`, `../jd-map.md`, `../people/`), and prep reads those. A round folder
that quotes a transcript directly has skipped the promotion step.

| ID | File | What it is | Date |
|---|---|---|---|

Row id: `src-YYYY-MM-DD-NN`. Name files `YYYY-MM-DD-{{what}}.{{ext}}` so the folder sorts by date.

## Not yet filed

Anything supplied in chat, sitting in a recordings folder, or living only in a mailbox. A row here is
a debt, not a note: chat context ends, recorders overwrite their rolling folders, and a mailbox is not
this system. Clear the list before the next round.

Folder created {date} by `af pipe start`.
