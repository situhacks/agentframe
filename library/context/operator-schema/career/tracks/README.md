# Tracks (schema)

One file per positioning track under `library/context/operator/career/tracks/` (e.g. `startup.md`, `enterprise.md`): which roles/titles the track targets, framing emphasis (what to lead with), tone notes, and any resume section-order deviation. The jd-map names the active track; the resume template reads it.
