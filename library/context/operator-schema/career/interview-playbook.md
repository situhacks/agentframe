# Interview playbook — schema

Copy to `library/context/operator/career/interview-playbook.md` and fill from real rounds. Standing and application-independent: the per-application post-mortem stays in its application folder, and only what **repeats across rounds** earns a line here.

This file exists because interview lessons decay. Two rounds weeks apart will derive the same failure independently unless one durable surface carries the first one forward.

**Read before every round. Append after every round.** Routes that load it: the careers pack's `production.md` interview step, the `interview-prep` deliverable template's Before Writing gate, and [`career-harvest`](../../../process/career-harvest.md) after a round.

## Evidence base

Name the rounds this file is derived from, each with a date and a pointer to the full analysis. A playbook with no named rounds is a guess, not a playbook.

- FILL-ME (date, company, round type, duration) → pointer to the retro or post-mortem

## The one rule

The single highest-cost pattern across rounds, stated as one sentence the operator will actually recall under pressure, plus the corrective phrased as a rule rather than an aspiration.

- FILL-ME

## The repeating patterns

One numbered entry per pattern that appeared in **more than one** round. Each carries the observed instance and a `Fix:` line. Delete a pattern once two consecutive rounds pass without it, rather than accumulating forever.

- FILL-ME

## Do-not-say list

| Never | Say instead |
|---|---|
| FILL-ME (the exact phrase used) | FILL-ME (the replacement, in the operator's own register) |

Verbatim phrases only. A paraphrase will not be recognised in the moment.

## Register by questioner

What altitude each kind of interviewer needs. Mismatched register reads as a capability gap even when the substance is right.

- FILL-ME (questioner type) → FILL-ME (what they need)

## The drill set

The openers to say **out loud** before a round, one per likely question theme, each citing its proof-point ID so the number is traceable and verified.

| When they ask about | Open with |
|---|---|
| FILL-ME (theme) | FILL-ME (the opener, number first) `pp-NNN` |

## Standing cautions

Claims that must not be made externally, or must be phrased exactly one way: unverified numbers, figures pending an external-safety check, work whose provenance would not survive a reference check, and any level or title claim not yet earned.

- FILL-ME

## Before every round

The fixed pre-round checklist, sized to the time it actually gets. Keep it short enough to complete.

1. FILL-ME

## What went right

Strengths to preserve, so a correction pass does not delete a working move along with a broken one.

- FILL-ME

## After every round

Append one dated block. Per-call detail stays in the application folder.

```
### YYYY-MM-DD — Company, round
- Pattern that repeated:
- New phrase for the do-not-say list:
- Drill-set row that worked / was missing:
```
